19. What is the rule set

A)The ruleset is used to identify that selectors can be attached with other selectors. It has two parts

Selector - Selector indicates the HTML element you want to style.Declaration Block - The declaration block can contain one or more declarations separated by a semicolon.
