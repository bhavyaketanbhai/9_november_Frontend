8.What is === operator

A)is a strict equality operator in JavaScript that compares two values for equality without performing type coercian.this means that it compares the values and the their types.